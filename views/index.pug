extends layout

block content
  h1= title
  p Welcome to #{title} JS Tutorial page
  button(type="button", class="btn btn-secondary" onclick='update()') press me 
  br
  br
  div(id='target') Display
  br
  a(href='/table') Search table page
  script.
    var client_count = 0;
    function update(){
      client_count++
      document.getElementById("target").innerHTML = `Value now ${client_count}`
    }